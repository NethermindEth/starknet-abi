def test_dispatcher_decoding(decoding_dispatcher):
    assert len(decoding_dispatcher.class_ids) == 2
    # assert bytes.fromhex('') in decoding_dispatcher.class_ids
    # assert bytes.fromhex('') in decoding_dispatcher.class_ids
